import RequireCss from '@helpers/RequireCss';

const Hero = () => {
  return (
    <section className="zzz-o-hero">
      <h1>This is Hero block</h1>
      <RequireCss path="b-hero" />
    </section>
  );
};

export default Hero;
